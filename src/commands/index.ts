import BurnCommand from './burn';
import MintCommand from './mint';
import RefreshCommand from "./refresh";

export default [BurnCommand, MintCommand, RefreshCommand];

export {
    BurnCommand,
    MintCommand,
    RefreshCommand
}